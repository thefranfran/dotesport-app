export interface WrapperAnimatedProps extends React.PropsWithChildren {
    /**
     * The delay before the animation starts
     */
    delay?: number;
    /**
     * The duration of the animation
     */
    duration?: number;
}
